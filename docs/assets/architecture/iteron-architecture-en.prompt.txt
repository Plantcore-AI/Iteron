Create a presentation-ready systems-paper architecture figure.

PURPOSE
- Title, verbatim: "ITERON · TARGET ARCHITECTURE"
- Intended message: "Reproduce the existing Iteron target-architecture figure exactly, changing only the former product name Core Code to Iteron while preserving every group, node, label, connector, color, and spatial relationship."

CANVAS
- Aspect ratio: "16:9 landscape"
- Background: "clean near-white technical paper"

VISUAL STYLE
- Medium: "formal systems-paper architecture diagram with precise flat boxes, restrained color, condensed technical typography, and no decorative illustration"
- Palette: "near-white ground; navy and cobalt runtime; green offline control; purple evolvable strategy; orange human promotion; red prohibition"

SEMANTIC ZONES — draw each zone exactly once as containment, never as an arrow endpoint:
- offline: "OFFLINE EVOLUTION CONTROL PLANE"; semantic tone: green
- runtime: "ITERON HARNESS BOUNDARY"; semantic tone: blue
- strategy: "EVOLVABLE HARNESS / STRATEGY SLOTS"; semantic tone: purple
- world: "REPLACEABLE WORLD / EXECUTION MODULES"; semantic tone: blue

NODE INVENTORY — draw each node exactly once; kind is semantic guidance only and must not be rendered:
- evidence: "EVIDENCE"; zone: offline; semantic kind, do not render: "governed runtime evidence"
- candidate_factory: "CANDIDATE FACTORY"; zone: offline; semantic kind, do not render: "container with Harness: Search · DSPy · Generated Code and Model: SFT · DPO · GRPO · RL"
- held_out_eval: "HELD-OUT EVAL"; zone: offline; semantic kind, do not render: "independent held-out evaluation"
- shadow: "SHADOW"; zone: offline; semantic kind, do not render: "shadow validation"
- canary: "CANARY"; zone: offline; semantic kind, do not render: "guarded canary"
- human_promotion: "HUMAN PROMOTION"; zone: offline; semantic kind, do not render: "human gate"
- strategy_bundle: "IMMUTABLE STRATEGY BUNDLE"; zone: offline; semantic kind, do not render: "released immutable bundle"
- microkernel: "FIXED MICROKERNEL / TRUSTED COMPUTING BASE"; zone: runtime; semantic kind, do not render: "Protocol · Reducer · Capability Admission · Budgets · Effect Broker · Record / Replay"
- harness_slots: "EVOLVABLE HARNESS / STRATEGY SLOTS"; zone: strategy; semantic kind, do not render: "three-by-three grid: Router, Planner, Context, Memory, Scheduler, Tool Policy, Verifier, Model Router, Collaboration"
- world_modules: "REPLACEABLE WORLD / EXECUTION MODULES"; zone: world; semantic kind, do not render: "single row: Providers, Filesystem, Shell + Git, Sandbox, MCP + LSP, Vertical Adapters"

ONLY ALLOWED SINGLE-HEADED ARROW LEDGER — draw every listed edge exactly once and no unlisted connector:
- IT-01: "EVIDENCE" (evidence) — "governed evidence" → "CANDIDATE FACTORY" (candidate_factory); tone: green; style: dashed; place the sole arrowhead at the receiver candidate_factory
- IT-02: "CANDIDATE FACTORY" (candidate_factory) — "candidate" → "HELD-OUT EVAL" (held_out_eval); tone: green; style: dashed; place the sole arrowhead at the receiver held_out_eval
- IT-03: "HELD-OUT EVAL" (held_out_eval) — "pass" → "SHADOW" (shadow); tone: green; style: dashed; place the sole arrowhead at the receiver shadow
- IT-04: "SHADOW" (shadow) — "pass" → "CANARY" (canary); tone: green; style: dashed; place the sole arrowhead at the receiver canary
- IT-05: "CANARY" (canary) — "pass" → "HUMAN PROMOTION" (human_promotion); tone: green; style: dashed; place the sole arrowhead at the receiver human_promotion
- IT-06: "HUMAN PROMOTION" (human_promotion) — "approved promotion" → "IMMUTABLE STRATEGY BUNDLE" (strategy_bundle); tone: green; style: dashed; place the sole arrowhead at the receiver strategy_bundle
- IT-07: "IMMUTABLE STRATEGY BUNDLE" (strategy_bundle) — "SLOW OFFLINE RELEASE LOOP" → "EVIDENCE" (evidence); tone: green; style: dashed; place the sole arrowhead at the receiver evidence
- IT-08: "IMMUTABLE STRATEGY BUNDLE" (strategy_bundle) — "activate pinned bundle" → "FIXED MICROKERNEL / TRUSTED COMPUTING BASE" (microkernel); tone: green; style: solid; place the sole arrowhead at the receiver microkernel
- IT-09: "FIXED MICROKERNEL / TRUSTED COMPUTING BASE" (microkernel) — "bounded strategy interface" → "EVOLVABLE HARNESS / STRATEGY SLOTS" (harness_slots); tone: blue; style: solid; place the sole arrowhead at the receiver harness_slots
- IT-10: "EVOLVABLE HARNESS / STRATEGY SLOTS" (harness_slots) — "capability-scoped execution proposal" → "REPLACEABLE WORLD / EXECUTION MODULES" (world_modules); tone: blue; style: solid; place the sole arrowhead at the receiver world_modules
- IT-11: "REPLACEABLE WORLD / EXECUTION MODULES" (world_modules) — "FAST ONLINE TASK LOOP" → "EVOLVABLE HARNESS / STRATEGY SLOTS" (harness_slots); tone: blue; style: solid; place the sole arrowhead at the receiver harness_slots
- IT-12: "REPLACEABLE WORLD / EXECUTION MODULES" (world_modules) — "runtime evidence" → "EVIDENCE" (evidence); tone: blue; style: solid; place the sole arrowhead at the receiver evidence

FORBIDDEN EDGES — never draw these source/receiver pairs:
- "CANDIDATE FACTORY" (candidate_factory) → "FIXED MICROKERNEL / TRUSTED COMPUTING BASE" (microkernel): "NO SELF-GRANTED AUTHORITY"
- "CANDIDATE FACTORY" (candidate_factory) → "EVOLVABLE HARNESS / STRATEGY SLOTS" (harness_slots): "Candidates cannot activate themselves."
- "HELD-OUT EVAL" (held_out_eval) → "IMMUTABLE STRATEGY BUNDLE" (strategy_bundle): "Shadow, canary, and human promotion cannot be bypassed."
- "CANARY" (canary) → "FIXED MICROKERNEL / TRUSTED COMPUTING BASE" (microkernel): "Canary cannot activate a strategy without human promotion and an immutable bundle."
- "EVOLVABLE HARNESS / STRATEGY SLOTS" (harness_slots) → "IMMUTABLE STRATEGY BUNDLE" (strategy_bundle): "The online harness cannot rewrite its own released strategy bundle."

LAYOUT REQUIREMENTS:
- "Use the supplied original PNG as the authoritative composition: preserve its geometry, proportions, typography hierarchy, containment, colors, and routing."
- "The top header is ITERON on the left, TARGET ARCHITECTURE in the middle, and THE HARNESS EVOLVES. AUTHORITY DOES NOT. on the right."
- "The offline control plane is a full-width green band above the blue runtime boundary."
- "Inside the runtime boundary, stack the fixed microkernel, the purple three-by-three strategy grid, and the blue one-by-six execution-module row."
- "Render IT-07 as the long dashed green return loop and IT-11 as the large blue perimeter feedback loop."
- "Show a red dashed attempted path from the offline plane toward the runtime, terminate it with a red X, and label it NO SELF-GRANTED AUTHORITY; this is a prohibition marker, not an allowed edge."
- "Do not add a bottom caption; place the caption only in its existing top-right header position."

REQUIRED TEXT — render each string verbatim:
- "ITERON"
- "TARGET ARCHITECTURE"
- "THE HARNESS EVOLVES. AUTHORITY DOES NOT."
- "Harness: Search · DSPy · Generated Code"
- "Model: SFT · DPO · GRPO · RL"
- "SLOW OFFLINE RELEASE LOOP"
- "NO SELF-GRANTED AUTHORITY"
- "Protocol · Reducer · Capability Admission · Budgets · Effect Broker · Record / Replay"
- "ROUTER"
- "PLANNER"
- "CONTEXT"
- "MEMORY"
- "SCHEDULER"
- "TOOL POLICY"
- "VERIFIER"
- "MODEL ROUTER"
- "COLLABORATION"
- "PROVIDERS"
- "FILESYSTEM"
- "SHELL + GIT"
- "SANDBOX"
- "MCP + LSP"
- "VERTICAL ADAPTERS"
- "FAST ONLINE TASK LOOP"

CAPTION
- Bottom caption, verbatim: "THE HARNESS EVOLVES. AUTHORITY DOES NOT."

CONNECTOR INTEGRITY — mandatory:
- Every connector must correspond to exactly one ledger edge above.
- Treat zone, node, and edge IDs as internal references; do not render the IDs.
- Render every zone label, node label, edge label, required-text string, title, and caption verbatim.
- Draw one conventional single-headed arrow per edge, with its arrowhead at the receiver and its tail visibly leaving the source.
- Draw no extra connectors, no dangling connectors, no double-headed arrows, and no shared-trunk connectors.
- Never terminate an arrow in whitespace, on a zone boundary, on a node that is not its receiver, or on another connector.
- Route opposite-direction edges in visibly separate lanes; never merge them into a single two-way line.
- Keep edge labels beside unobstructed line segments and keep arrowheads clear of all text.
- If routing becomes crowded, increase whitespace or simplify node interiors; never reverse, merge, omit, duplicate, or invent an edge.
