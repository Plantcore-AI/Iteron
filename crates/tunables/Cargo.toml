[package]
name = "iteron-tunables"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[features]
# Test-only projection of production fixed-authority receipts. Downstream fixture crates opt in;
# release builds expose no API that can mint these receipts without executing owner adapters.
test-fixtures = []

[dependencies]
hex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
